interface:
  display_name: "Lead Studio"
  short_description: "Piloter la recherche de prospects de bout en bout"
  default_prompt: "Utilise $lead-studio pour trouver et qualifier mes prospects dans le mode de présentation configuré."
dependencies:
  tools:
    - type: "mcp"
      value: "lead-studio"
      description: "Outils locaux de recherche et de revue Lead Studio"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
