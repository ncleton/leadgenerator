interface:
  display_name: "Synchronisation HubSpot"
  short_description: "Ajouter une liste et l'attribuer à un collègue"
  default_prompt: "Utilise $lead-hubspot-sync pour ajouter ma sélection à une liste HubSpot et l'attribuer à un collègue."
dependencies:
  tools:
    - type: "mcp"
      value: "lead-studio"
      description: "Outils locaux de recherche et de revue Lead Studio"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
